Put your public experiment assets here.

Suggested contents:

- `teaser.mp4`
- `multiview-prediction.mp4`
- `coordination-rollout.mp4`
- `role-ablation.mp4`
- `camera-ablation.mp4`

The current page uses graphical placeholders so the site still looks complete before real media is added.
